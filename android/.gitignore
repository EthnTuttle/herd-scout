# Gradle outputs
.gradle/
app/build/
build/
local.properties
.idea/
*.iml
captures/

# cargo-ndk drops .so files here; regenerated each build.
app/src/main/jniLibs/

# gradle-wrapper.jar is downloaded by `gradle wrapper` (or fetched on first
# `./gradlew` run). Keeping it out of git keeps the tree small.
gradle/wrapper/gradle-wrapper.jar
